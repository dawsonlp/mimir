# Mímir - Environment Configuration
# Copy this file to .env and fill in your values
# .env is gitignored and should never be committed

# =============================================================================
# DATABASE
# =============================================================================

# PostgreSQL password (REQUIRED - NO DEFAULT)
# Generate a secure password with: openssl rand -base64 32
# 
# Password Requirements:
#   - Minimum 16 characters
#   - Mix of uppercase, lowercase, numbers, and symbols
#   - No dictionary words or common patterns
#   - Never commit actual passwords to git
#
POSTGRES_PASSWORD=

# Database connection URL (constructed from above, or override entirely)
# DATABASE_URL=postgresql://mimir:${POSTGRES_PASSWORD}@localhost:35432/mimir

# =============================================================================
# DOCKER BUILD
# =============================================================================

# Docker BuildX builder name for multi-architecture builds
# Create your builder with: docker buildx create --name my-builder --use
DOCKER_BUILDX_BUILDER=default

# =============================================================================
# APPLICATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Environment: development, production
ENVIRONMENT=development

# =============================================================================
# EMBEDDING PROVIDERS
# =============================================================================

# At least one embedding provider API key is required for embedding functionality.
# Voyage AI is recommended by Anthropic and preferred when configured.

# Voyage AI API Key (RECOMMENDED - Anthropic's recommended embedding provider)
# Get your key at: https://www.voyageai.com/
# Available models: voyage-3, voyage-3-large, voyage-3-lite, voyage-code-3,
#                   voyage-finance-2, voyage-law-2, voyage-multilingual-2
VOYAGEAI_MIMIR_EMBEDDINGS=

# OpenAI API Key (OPTIONAL - alternative embedding provider)
# Get your key at: https://platform.openai.com/api-keys
# Available models: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
OPENAI_API_KEY=

# Default embedding model (OPTIONAL - auto-detected from configured providers if not set)
# Examples: voyage-3, voyage-3-large, text-embedding-3-small
# If not set, prefers Voyage AI models when VOYAGE_API_KEY is configured
# DEFAULT_EMBEDDING_MODEL=voyage-3

# Embedding batch size (1-2048, default: 100)
# Maximum number of texts to embed in a single API call
# EMBEDDING_BATCH_SIZE=100

# =============================================================================
# OLLAMA (LOCAL EMBEDDINGS)
# =============================================================================

# Ollama server URL for local embedding models (no API key required)
# Default: http://localhost:11434
# OLLAMA_BASE_URL=http://localhost:11434

# Available Ollama embedding models (install with: ollama pull <model>):
#   - nomic-embed-text: 768 dims, 8K tokens, good balance (274 MB)
#   - mxbai-embed-large: 1024 dims, high quality (670 MB)
#   - snowflake-arctic-embed: 1024 dims, 8K tokens (669 MB)
#   - all-minilm: 384 dims, fast & lightweight (46 MB)
#   - bge-large: 1024 dims, good for RAG (670 MB)
#
# To use Ollama models: set DEFAULT_EMBEDDING_MODEL=nomic-embed-text
# Note: Ollama runs locally, so ensure ollama serve is running
